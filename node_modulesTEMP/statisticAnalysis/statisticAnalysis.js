module.exports = function(RED) {
    function statisticAnalysis(config) {
    
        RED.nodes.createNode(this,config);
        var node = this;
        
        var R = require("r-script");

        node.on('input', function(msg) {
        
        	/*
        	R("node_modules/statisticAnalysis/test.r").data(msg.payload).call(function(err, out) {
        		if (!err) {
        			
        		}
        		else {
        			// manage errors
        		}
        	});           
        	*/
        	node.send(msg);
            
        });
        
		node.on('close', function() {
		});
    }
    RED.nodes.registerType("statisticAnalysis",statisticAnalysis);
}