[
  {
	"url": "/log",
	"query": "select userid as id,count(*) as log from moodle.mdl_logstore_standard_log where userid >= 0 and userid <= 7 group by userid;",
	"source": "Moodle",
	"description": "Accessi degli utenti",
	"chartOptions": [
		{
			"label": "accessi",
			"type": "bar",
			"icon": "stats"
		}
	]
  },
  {
	"url": "/log2",
	"query": "select userid as id,count(*) as log from moodle.mdl_logstore_standard_log where userid >= 8 group by userid;",
	"source": "Moodle",
	"description": "Accessi degli utenti 2",
	"chartOptions": [
		{
			"label": "accessi",
			"type": "line",
			"icon": "analytics"
		}
	]
  },
  {
	"url": "/log3",
	"query": "select userid as id,count(*) as log from moodle.mdl_logstore_standard_log where userid >= 8 group by userid;",
	"source": "Moodle",
	"description": "Accessi degli utenti 3",
	"chartOptions": [
		{
			"label": "accessi",
			"type": "bar",
			"icon": "stats"
		},
		{
			"label": "accessi",
			"type": "line",
			"icon": "analytics"
		}
	]
  }
]